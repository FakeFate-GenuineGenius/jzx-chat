# 芯聊项目代码说明

本文档面向零基础读者，按文件解释这个名为“芯聊”的小聊天项目是怎么工作的。你可以把它当成一份“看代码说明书”。

## 先看整体结构

这个项目主要分成 4 个部分：

- `package.json`：项目配置文件，告诉 Node.js 这个项目叫什么、依赖哪些库、怎么运行。
- `server.js`：后端服务，负责开网页、处理房间、处理消息、处理掉线。
- `public/index.html`：前端页面，负责显示界面、输入昵称、输入房间号、发送消息。
- `public/style.css`：样式文件，但当前页面里主要样式已经写在 `index.html` 的 `<style>` 里，所以这个文件现在基本没有被实际引用。
- `logs/YYYY-MM-DD.log` 这种按天命名的聊天记录文件：服务器会自动把消息按时间写进去。
- `logs/`：聊天记录目录，已经加入 `.gitignore`，不会被 Git 上传。

> 你现在看到的聊天功能，本质上是“网页前端 + Node 后端 + Socket.IO 实时通信”三者一起工作。

---

## 一、`package.json` 说明

这个文件是项目的“身份证”和“依赖清单”。

```json
{
  "name": "jzx-chat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  }
}
```

### 逐项解释

- `name`: 项目名字，叫 `jzx-chat`。
- `version`: 项目版本号，当前是 `1.0.0`。
- `description`: 项目描述，现在是空的。
- `main`: 默认入口文件。这里写的是 `index.js`，但你这个项目实际运行的是 `server.js`，所以这个字段目前没有太大作用。
- `scripts`: 常用命令。
  - `test`: 这里只是默认占位，没有真正测试。
- `keywords`: 关键词列表，现在是空的。
- `author`: 作者信息，现在没写。
- `license`: 开源协议，`ISC` 是一种常见协议。
- `type`: `commonjs` 表示这个项目使用 Node.js 的 CommonJS 写法，也就是 `require(...)` 这种导入方式。
- `dependencies`: 项目运行时需要的第三方库。
  - `express`: 用来创建网页服务器。
  - `socket.io`: 用来实现网页和服务器之间的实时通信。

### 你要记住的重点

这个项目最重要的依赖就是：

- `express`：负责“把网页提供给浏览器”。
- `socket.io`：负责“浏览器和服务器实时聊天”。
- `logs/YYYY-MM-DD.log` 这种按天命名的文件：负责“把聊天内容按时间永久写到文件里”。

---

## 二、`server.js` 逐行讲解

`server.js` 是后端核心。它负责：

- 启动网站服务
- 提供前端页面
- 管理多个房间
- 管理每个房间里的两个人
- 转发聊天消息
- 把双方聊天内容写入按天命名的日志文件，并自动创建 `logs/` 目录
- 处理刷新、离线、主动退出、超时清理

下面按代码顺序解释。

### 1. 引入模块

```js
const express = require("express");
const http = require("http");
const { Server } = require("socket.io");
const crypto = require("crypto");
```

- `express`：创建网页服务器。
- `http`：Node.js 自带的 HTTP 模块，用来包裹 Express。
- `Server`：Socket.IO 的服务端对象。
- `crypto`：用来生成随机房间号。

### 2. 创建服务器

```js
const app = express();
const server = http.createServer(app);
const io = new Server(server);
```

- `app = express()`：创建一个 Express 应用。
- `http.createServer(app)`：用 Express 创建一个 HTTP 服务器。
- `new Server(server)`：把 Socket.IO 挂到这个 HTTP 服务器上。

你可以理解成：

- `express` 负责网页内容。
- `socket.io` 负责实时通信。
- 两者共享同一个服务器。

### 3. 设置自动退出时间

```js
const AUTO_LOGOUT_MS = 30 * 60 * 1000;
```

这行表示：

- 30 分钟 = 30 × 60 × 1000 毫秒
- 如果用户断线后 30 分钟内没有重新回来，就自动退出房间。

### 4. 提供静态资源

```js
app.use(express.static("public"));
```

这行的意思是：

- 把 `public` 文件夹当作静态资源目录。
- 浏览器访问网页时，可以直接拿到 `public/index.html`、图片、CSS 等文件。

### 5. 房间数据结构

```js
const rooms = new Map(); // roomCode -> { users: [] }
```

这里是整个项目最重要的数据结构之一。

- `rooms` 是一个 `Map`。
- 它的键是房间号，比如 `AB12CD`。
- 它的值是该房间的数据对象，里面存用户列表。

也就是说：

- 一个房间码对应一个房间。
- 每个房间最多 2 个人。

### 6. 生成随机房间号

```js
function generateRoomCode(length = 6) {
    const alphabet = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789";
    let code = "";
    for (let i = 0; i < length; i += 1) {
        code += alphabet[crypto.randomInt(0, alphabet.length)];
    }
    return code;
}
```

这段代码的作用是随机生成一个 6 位房间号。

逐句解释：

- `length = 6`：默认长度是 6 位。
- `alphabet`：可用字符表。
  - 这里故意去掉了容易混淆的字符，比如 `O`、`0`、`I`、`1`。
- `code = ""`：先准备一个空字符串。
- `for` 循环：循环 6 次，每次随机取一个字符。
- `crypto.randomInt(0, alphabet.length)`：生成一个随机下标。
- 最后返回拼好的房间码。

### 7. 保证房间号不重复

```js
function createRoomCode() {
    let roomCode = generateRoomCode();
    while (rooms.has(roomCode)) {
        roomCode = generateRoomCode();
    }
    return roomCode;
}
```

这段代码的作用是：

- 先生成一个随机房间号。
- 如果这个房间号已经存在，就重新生成。
- 直到生成一个没被使用过的房间号。

这样可以避免两个房间拿到同一个编号。

### 8. 过滤掉不需要传给前端的字段

```js
function getPublicUsers(room) {
    return room.users.map(({ disconnectTimer, ...user }) => user);
}
```

这段代码的作用是：

- 房间里的用户对象有很多字段。
- 其中 `disconnectTimer` 是服务器内部用的，不需要发给前端。
- 所以这里把它去掉，只把普通用户信息发给浏览器。

`{ disconnectTimer, ...user }` 的意思是：

- 把 `disconnectTimer` 单独拿出来丢掉。
- 剩下的内容放进 `user`。

### 9. 广播房间状态

```js
function emitRoomState(roomCode) {
    const room = rooms.get(roomCode);
    if (!room) return;
    io.to(roomCode).emit("room-state", {
        roomCode,
        users: getPublicUsers(room),
    });
}
```

这段代码的作用是：

- 找到对应房间。
- 如果房间存在，就向这个房间里的所有 socket 发一个 `room-state` 事件。
- 前端收到后会更新“谁在线、谁离线、房间号是什么”。

重点：

- `io.to(roomCode)` 表示只发给这个房间里的人。
- 不会发给其他房间。

### 10. 如果房间没人了就删除房间

```js
function deleteRoomIfEmpty(roomCode) {
    const room = rooms.get(roomCode);
    if (!room || room.users.length > 0) return;
    rooms.delete(roomCode);
}
```

这段代码的意思很直接：

- 如果房间存在，但里面已经没人了，就把这个房间从 `rooms` 里删掉。
- 这样可以避免内存里堆积很多空房间。

### 11. 从房间里移除某个用户

```js
function removeUser(roomCode, sessionId) {
    const room = rooms.get(roomCode);
    if (!room) return;

    const index = room.users.findIndex((user) => user.sessionId === sessionId);
    if (index === -1) return;

    const [removed] = room.users.splice(index, 1);
    if (removed.disconnectTimer) {
        clearTimeout(removed.disconnectTimer);
    }

    deleteRoomIfEmpty(roomCode);
}
```

这段代码的作用：

- 根据房间号和用户会话 ID，把这个用户删掉。
- 如果这个用户有“30 分钟后自动退出”的定时器，也一并清除。
- 删除后检查房间是不是空了，如果空了就删除房间。

### 12. 把用户绑定到房间

```js
function bindUserToRoom(socket, roomCode, userData, { createIfMissing = false } = {}) {
```

这是一个非常核心的函数，负责“用户加入房间”。

参数解释：

- `socket`：当前连接的用户。
- `roomCode`：要加入的房间号。
- `userData`：用户信息，比如昵称、头像、会话 ID。
- `createIfMissing`：如果房间不存在，是否允许创建。

#### 12.1 检查用户信息是否完整

```js
    if (!userData || !userData.user || !userData.sessionId) {
        return { ok: false, error: "缺少登录信息，请刷新后重新进入" };
    }
```

意思是：

- 如果没有昵称或会话 ID，就不能加入房间。
- 直接返回错误。

#### 12.2 找到房间，不存在就看是否允许创建

```js
    let room = rooms.get(roomCode);
    if (!room) {
        if (!createIfMissing) {
            return { ok: false, error: "房间不存在，请确认房间密码" };
        }
        room = { users: [] };
        rooms.set(roomCode, room);
    }
```

这段逻辑是：

- 先找房间。
- 如果没找到：
  - `createIfMissing = false`，那就报错，因为这个模式是“只加入已有房间”。
  - `createIfMissing = true`，那就新建一个房间。

### 12.3 检查房间是否已满

```js
    const existingUser = room.users.find((user) => user.sessionId === userData.sessionId);
    if (room.users.length >= 2 && !existingUser) {
        return { ok: false, error: "房间已满，仅允许两人聊天" };
    }
```

意思是：

- 一个房间最多 2 个人。
- 如果已经 2 个人了，且当前用户不是房间里原本的人，就不能加入。

### 12.4 如果是老用户回来，就恢复连接

```js
    if (existingUser) {
        if (existingUser.disconnectTimer) {
            clearTimeout(existingUser.disconnectTimer);
            existingUser.disconnectTimer = null;
        }
        existingUser.id = socket.id;
        existingUser.user = userData.user;
        existingUser.avatar = userData.avatar;
        existingUser.online = true;
    }
```

这段是在处理“刷新后回来”的情况。

- 如果这个会话 ID 已经存在，说明这是同一个人回来。
- 先清掉旧的自动退出定时器。
- 再更新 socket id。
- 再把状态标记为在线。

### 12.5 如果是新用户，就加入房间

```js
    } else {
        room.users.push({
            sessionId: userData.sessionId,
            id: socket.id,
            user: userData.user,
            avatar: userData.avatar,
            online: true,
            disconnectTimer: null,
        });
    }
```

这表示：

- 如果不是老用户，就新建一个用户对象。
- 保存他的会话 ID、socket ID、昵称、头像、在线状态等。

### 12.6 把 socket 加进房间

```js
    socket.join(roomCode);
    socket.userData = { ...userData, roomCode };
    socket.roomCode = roomCode;
```

这三句非常重要：

- `socket.join(roomCode)`：让当前连接进入这个房间组。
- `socket.userData`：把用户资料挂到 socket 上，后面断线时还能用。
- `socket.roomCode`：记录当前 socket 属于哪个房间。

### 12.7 返回结果

```js
    return { ok: true, roomCode, users: getPublicUsers(room) };
```

表示绑定成功，并把房间信息返回给调用者。

---

## 13. 监听连接事件

```js
io.on("connection",(socket)=>{
```

当浏览器连上 Socket.IO 时，就会进入这个回调。

你可以理解成：

- 每来一个新用户，这里就创建一个 socket 连接。

### 13.1 创建房间

```js
    socket.on("create-room",(userData)=>{
        const roomCode = createRoomCode();
        const result = bindUserToRoom(socket, roomCode, userData, { createIfMissing: true });
        if (!result.ok) {
            socket.emit("room-error", result.error);
            return;
        }

        socket.emit("room-created", { roomCode: result.roomCode });
        emitRoomState(result.roomCode);
        console.log("房间创建:", result.roomCode, userData.user);
    });
```

作用：

- 前端说“我要创建房间”。
- 服务器随机生成一个房间号。
- 把当前用户加入这个新房间。
- 告诉前端房间号是多少。
- 广播这个房间当前状态。

### 13.2 加入房间

```js
    socket.on("join-room",(payload)=>{
        const result = bindUserToRoom(socket, payload?.roomCode, payload, { createIfMissing: false });
        if (!result.ok) {
            socket.emit("room-error", result.error);
            return;
        }

        emitRoomState(result.roomCode);
        console.log("用户加入:", result.roomCode, payload.user);
    });
```

作用：

- 前端输入房间号后，会请求加入这个房间。
- 如果房间不存在，就报错。
- 如果房间已满，也报错。
- 加入成功后广播房间状态。

### 13.3 主动退出

```js
    socket.on("leave-room", (payload = {}) => {
        const roomCode = payload.roomCode || socket.roomCode || socket.userData?.roomCode;
        const sessionId = payload.sessionId || socket.userData?.sessionId;
        if (!roomCode || !sessionId) return;

        removeUser(roomCode, sessionId);
        emitRoomState(roomCode);
        console.log("用户主动退出:", roomCode, socket.userData ? socket.userData.user : sessionId);
    });
```

作用：

- 用户点击“主动退出”时触发。
- 服务器把用户从房间里删除。
- 广播最新状态。
- 如果房间空了，房间会被删除。

### 13.4 转发聊天消息

```js
    socket.on("chat message",(msgObj)=>{
        const roomCode = socket.roomCode || socket.userData?.roomCode || msgObj.roomCode;
        if (!roomCode) return;

        const room = rooms.get(roomCode);
        if (!room) return;

        room.users.forEach((user) => {
            if (user.id !== socket.id && user.online) {
                io.to(user.id).emit("chat message", msgObj);
            }
        });
    });
```

作用：

- 当一个人发消息时，服务器把消息转发给同房间的另一个人。
- 不会转发给自己。
- 不会转发给其他房间的人。

### 13.5 断线处理

```js
    socket.on("disconnect",()=>{
        const roomCode = socket.roomCode || socket.userData?.roomCode;
        if (!roomCode) return;

        const room = rooms.get(roomCode);
        if (!room) return;

        const disconnectedUser = room.users.find((user) => user.id === socket.id);
        if (!disconnectedUser) return;

        disconnectedUser.id = null;
        disconnectedUser.online = false;
        disconnectedUser.disconnectTimer = setTimeout(() => {
            const sessionId = disconnectedUser.sessionId;
            const userName = disconnectedUser.user;
            removeUser(roomCode, sessionId);
            emitRoomState(roomCode);
            console.log("用户超时退出:", roomCode, userName);
        }, AUTO_LOGOUT_MS);

        emitRoomState(roomCode);
        console.log("用户离开:", roomCode, socket.userData ? socket.userData.user : socket.id);
    });
```

作用：

- 当浏览器刷新、关闭标签页、断网时，会触发 `disconnect`。
- 服务器不会马上删人，而是：
  - 把用户标记成离线。
  - 开一个 30 分钟倒计时。
  - 如果 30 分钟内回来，就恢复。
  - 如果没回来，就自动退出。

这就是“刷新不掉线”的关键逻辑。

---

## 14. 启动服务器

```js
server.listen(3000, "0.0.0.0", () => {
    console.log("Server running on http://192.168.11.84:3000");
});
```

这行的意思是：

- 监听 3000 端口。
- `0.0.0.0` 表示允许局域网内其他设备访问。
- 启动成功后输出一条日志。

---

## 三、`public/index.html` 逐行讲解

这个文件是前端页面。你在浏览器里看到的所有内容，基本都在这里。

### 1. HTML 基本结构

```html
<!DOCTYPE html>
<html lang="zh">
<head>
```

- `<!DOCTYPE html>`：告诉浏览器这是 HTML5 页面。
- `<html lang="zh">`：页面语言是中文。
- `<head>`：网页头部，放标题、样式、元信息。

### 2. 页面基础信息

```html
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>芯聊</title>
```

- `charset="UTF-8"`：支持中文。
- `viewport`：让手机也能正常显示。
- `title`：浏览器标签页标题。

### 3. 样式

你现在把大部分样式都写在页面内部的 `<style>` 里。

比如：

```css
body { margin:0; font-family:"微软雅黑",Arial,sans-serif; background:#f0f2f5; }
```

意思是：

- 去掉默认边距。
- 设置字体。
- 设置页面背景色。

还有：

```css
#setup { ... }
```

这是“登录/设置区域”的样式。

```css
.chat-container { display:none; flex-direction:column; height:100vh; }
```

这是聊天界面的容器，默认隐藏，进入房间后才显示。

```css
#online-status { ... }
```

这是顶部状态栏，显示在线状态和房间号。

```css
#messages { ... }
```

这是聊天消息列表区域。

```css
.message.self / .message.other
```

这两类用于区分“自己发的消息”和“对方发的消息”。

### 4. 页面主体

页面分成两块：

#### 4.1 设置界面

```html
<div id="setup">
    <h2>输入你的昵称</h2>
    <input id="usernameInput" placeholder="昵称" />
    <h2>房间密码</h2>
    <input id="roomCodeInput" placeholder="留空自动创建随机房间" />
    <h2>选择头像</h2>
```

这里让用户输入：

- 昵称
- 房间密码
- 选择头像

如果房间密码留空，就自动创建一个新房间。

头像列表：

```html
<div id="avatar-options">
    <img src="https://i.pravatar.cc/50?img=1">
    <img src="https://i.pravatar.cc/50?img=2">
    <img src="https://i.pravatar.cc/50?img=3">
    <img src="https://i.pravatar.cc/50?img=4">
</div>
```

这些头像图片来自在线头像服务。

```html
<button id="startBtn">开始聊天</button>
```

点击后开始进入房间。

#### 4.2 聊天界面

```html
<div class="chat-container" id="chatContainer">
    <div id="online-status">
        <span id="statusText">等待另一位用户上线...</span>
        <div id="statusMeta">
            <span id="roomBadge">房间：-</span>
            <button id="exitBtn" type="button">主动退出</button>
        </div>
    </div>
```

顶部状态栏里有：

- 状态文字
- 房间号
- 主动退出按钮

下面是消息区：

```html
<div id="messages"></div>
```

再下面是输入框：

```html
<div class="input-box">
    <input id="messageInput" placeholder="输入消息..." />
    <button id="sendBtn">发送</button>
</div>
```

### 5. 引入 Socket.IO

```html
<script src="/socket.io/socket.io.js"></script>
```

这行非常关键。

- 它让浏览器端可以使用 `io()`。
- 这样前端才能和后端实时通信。

### 6. 前端变量

```js
const STORAGE_KEY = "jzx-chat-profile";
let myUsername = "";
let myAvatar = "";
let mySessionId = "";
let myRoomCode = "";
let roomUsers = [];
let hasJoinedRoom = false;
const socket = io();
```

逐个解释：

- `STORAGE_KEY`：本地存储的键名。
- `myUsername`：当前用户昵称。
- `myAvatar`：当前用户头像。
- `mySessionId`：当前用户会话 ID。
- `myRoomCode`：当前房间号。
- `roomUsers`：当前房间用户列表。
- `hasJoinedRoom`：防止重复加入房间。
- `socket = io()`：建立 Socket.IO 连接。

### 7. 获取页面元素

```js
const setupDiv = document.getElementById("setup");
const chatContainer = document.getElementById("chatContainer");
const usernameInput = document.getElementById("usernameInput");
const roomCodeInput = document.getElementById("roomCodeInput");
const avatarOptions = document.getElementById("avatar-options").getElementsByTagName("img");
```

这些代码是在把页面上的元素找出来，后面才能操作它们。

### 8. 生成会话 ID

```js
function createSessionId(){
    if (window.crypto && typeof window.crypto.randomUUID === "function") {
        return window.crypto.randomUUID();
    }
    return `session-${Date.now()}-${Math.random().toString(16).slice(2)}`;
}
```

作用：

- 给每个用户生成一个唯一身份。
- 刷新页面后仍可识别这是“同一个人”。

### 9. 本地存储

```js
function loadProfile(){ ... }
function saveProfile(){ ... }
function clearProfile(){ ... }
```

这三个函数负责：

- 读取本地保存的数据
- 保存当前用户信息
- 清除本地保存的数据

这样刷新页面后能自动恢复昵称、头像、房间号。

### 10. 头像选择

```js
function setAvatarSelection(src){
    selectedAvatar = src;
    for(let i of avatarOptions) {
        i.classList.toggle("selected", i.src === selectedAvatar);
    }
}
```

作用：

- 点击头像后，选中当前头像。
- 给当前选中的头像加上高亮边框。

### 11. 显示和隐藏页面

```js
function showChat(){ ... }
function showSetup(){ ... }
```

作用：

- `showChat()`：隐藏设置页，显示聊天页。
- `showSetup()`：隐藏聊天页，显示设置页。

### 12. 房间号显示

```js
function updateRoomBadge(){
    roomBadge.textContent = myRoomCode ? `房间：${myRoomCode}` : "房间：-";
}
```

作用：

- 如果已经有房间号，就显示房间号。
- 如果没有，就显示 `房间：-`。

### 13. 在线状态提示

```js
function updateStatus(users){
```

这个函数会根据房间里的其他用户状态，显示：

- 对方已上线
- 对方暂时离线
- 等待另一位用户上线

### 14. 加入房间

```js
function joinRoom(){
    if (!myUsername || !myAvatar || !mySessionId || hasJoinedRoom) {
        return;
    }

    hasJoinedRoom = true;
    const payload = { user: myUsername, avatar: myAvatar, sessionId: mySessionId };
    if (myRoomCode) {
        socket.emit("join-room", { ...payload, roomCode: myRoomCode });
    } else {
        socket.emit("create-room", payload);
    }
}
```

这段代码决定：

- 如果输入了房间号，就加入这个房间。
- 如果没输入，就创建新房间。

### 15. 主动退出

```js
function resetCurrentUser({ keepSocket = true } = {}){ ... }
```

作用：

- 清空当前用户信息。
- 清空聊天内容。
- 返回设置页。
- 通知服务器主动退出。

### 16. 点击头像

```js
for(let img of avatarOptions){
    img.addEventListener("click", ()=>{
        setAvatarSelection(img.src);
    });
}
```

作用：

- 用户点击哪张头像，就选哪张。

### 17. 页面加载时自动恢复

```js
const savedProfile = loadProfile();
if (savedProfile && savedProfile.user && savedProfile.avatar && savedProfile.sessionId) {
    ...
}
```

作用：

- 如果本地存过资料，就自动恢复。
- 这样刷新后不会丢失昵称、头像、房间号。

### 18. 开始聊天按钮

```js
startBtn.addEventListener("click", ()=>{ ... });
```

作用：

- 读取昵称和房间号。
- 生成会话 ID。
- 调用 `joinRoom()` 进入或创建房间。

### 19. 消息发送

```js
btn.addEventListener("click", ()=>{ ... });
```

作用：

- 从输入框拿到消息。
- 发给服务器。
- 自己本地也立即显示一份。

### 20. 回车发送

```js
input.addEventListener("keydown",(e)=>{ if(e.key==="Enter") btn.click(); });
```

作用：

- 按回车键也能发送消息。

### 21. Socket 连接后自动重连

```js
socket.on("connect", ()=> { ... });
```

作用：

- 如果页面刷新后 socket 重新连上，自动重新加入房间。

### 22. 断线提示

```js
socket.on("disconnect",()=>{ ... });
```

作用：

- 断线时提示用户。
- 但不会立刻把人踢出，因为服务器会保留 30 分钟。

### 23. 接收消息

```js
socket.on("chat message",(msgObj)=>{ ... });
```

作用：

- 收到对方消息后，显示到页面里。
- 过滤掉自己重复发来的消息。

### 24. 房间创建成功

```js
socket.on("room-created", ({ roomCode }) => { ... });
```

作用：

- 创建房间成功后，服务器返回房间号。
- 前端把房间号保存起来并显示。

### 25. 房间状态更新

```js
socket.on("room-state", ({ roomCode, users }) => { ... });
```

作用：

- 服务器告诉前端当前房间里有哪些人。
- 前端更新状态栏。
- 同步房间号和在线状态。

### 26. 房间错误

```js
socket.on("room-error",(msg)=>{ ... });
```

作用：

- 如果房间不存在、房间满了、登录信息缺失，就弹出错误提示。

---

## 四、`public/style.css` 说明

这个文件内容目前是：

- 页面基础字体
- 聊天框布局
- 滚动条样式
- 消息气泡样式
- 输入框和按钮样式

但是要注意：

### 现在这个文件并没有真正被页面引用

你的 `index.html` 里目前没有这一句：

```html
<link rel="stylesheet" href="/style.css">
```

所以浏览器实际使用的，主要是 `index.html` 里自己写的 `<style>`。

### `style.css` 里有一部分设计思路

比如：

- 左侧用户列表
- 右侧聊天框
- 消息列表

但是当前 `index.html` 里已经做了另一套布局，所以这个文件现在更像“旧样式残留”或者“备用样式”。

### 如果你以后想整理项目

你可以把样式统一成一种方式：

- 要么全部放进 `index.html`
- 要么全部放进 `style.css`

现在这种“两边都有样式”的情况，容易让人困惑。

---

## 五、整个项目的运行流程

你可以把它理解成下面这几个步骤：

### 第一步：打开网页

浏览器访问 `server.js` 提供的页面，加载 `index.html`。

### 第二步：输入昵称和房间号

- 留空房间号：自动创建新房间
- 输入房间号：加入已有房间

### 第三步：前端连接 Socket.IO

网页和服务器建立实时连接。

### 第四步：服务器创建或加入房间

- 如果是新房间，服务器生成随机房间码
- 如果是旧房间，就把用户放进去

### 第五步：开始聊天

发送的消息由服务器转发给同房间的另一个人。

### 第六步：刷新或断线

- 服务器不会立刻踢人
- 会先保留 30 分钟
- 这 30 分钟内刷新回来可以继续用

### 第七步：主动退出

如果用户点“主动退出”，服务器会立即清理该用户。

---

## 六、给零基础读者的几个重点概念

### 1. 什么是前端

前端就是你在浏览器里看到的页面。

### 2. 什么是后端

后端就是服务器程序，负责存数据、处理消息、控制房间。

### 3. 什么是实时通信

普通网页要刷新才能看到新内容。

Socket.IO 可以让网页和服务器“随时说话”，消息一发出就能立刻显示。

### 4. 什么是房间

房间就是一个聊天小组。

这个项目里一个房间最多 2 个人。

### 5. 什么是会话 ID

会话 ID 是用来识别“同一个人”的。

这样即使页面刷新了，服务器也知道这是原来的那个人回来了。

---

## 七、这份代码里最值得你记住的地方

如果你只想先理解最核心的 3 件事：

1. `server.js` 负责“房间和消息的逻辑”。
2. `index.html` 负责“页面和交互”。
3. `socket.io` 负责“前后端实时通信”。

---

## 八、如果你想继续学，我建议的顺序

你可以按这个顺序继续看：

1. 先看 `index.html`，理解页面怎么渲染、按钮怎么工作。
2. 再看 `server.js`，理解房间怎么创建、消息怎么转发。
3. 最后看 Socket.IO 的工作方式，理解为什么消息能实时出现。

---

## 九、一个很实用的小提醒

当前项目虽然能用，但还有一个结构上的小问题：

- `style.css` 没有被 `index.html` 引用
- `index.html` 里又自己写了一套样式

如果以后你想把项目整理得更规范，建议把样式统一到一个地方。

---

## 十、总结

这个项目的本质就是：

- 浏览器页面收集昵称、头像和房间号
- 服务器按房间号管理两个人
- Socket.IO 负责把消息实时转发
- 刷新不会立刻掉线，断线 30 分钟内可恢复

如果你愿意，我下一步可以继续帮你做两件事中的任意一个：

1. 把这份文档再整理成“更像新手教程”的版本，加入更多示意图和流程图说明。
2. 继续给你画出这个项目的“运行流程图”，让你更容易看懂前后端怎么配合。
