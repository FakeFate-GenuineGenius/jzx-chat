# 操作指令

每次重开时，按下面的顺序执行：

1. 先启动聊天服务，保持 `server.js` 运行。
2. 再启动穿透地址，执行：

```bash
cloudflared tunnel --url http://localhost:3000
```

3. 等终端输出新的公网网址，把这个新网址发到手机上打开。

## 注意事项

- 这个 quick tunnel 每次重开地址都会变，所以不能长期固定用同一个网址。
- 如果你只是关了浏览器再打开，而电脑上的服务和穿透终端还开着，就不用重新生成地址，直接打开当前那个网址就行。

## 停止方式

通常有两种：

- 如果它是在终端里启动的，回到那个终端按 `Ctrl+C`。
- 如果它作为后台进程在跑，打开 PowerShell 执行：

```powershell
Stop-Process -Name cloudflared -Force
```